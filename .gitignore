node_modules/
dist/
dist-ssr/
*.local
.DS_Store
*.tsbuildinfo

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# App icons ARE committed (so the desktop app builds out of the box).
# Regenerate them from a source PNG with: npm run tauri icon <png>

# Real broker exports dropped in for the import-coverage harness stay private (parsed
# locally, never committed). The README is kept so the folder + instructions exist.
samples/*
!samples/README.md

# Scratch dir for real exports under active debugging — never committed.
testfiles/

# Personal PII patterns for the pre-commit scanner — never commit
scripts/.pii-denylist

# Apple signing/notarization credentials — never commit
.env.signing
